cache: bundler
language: ruby
sudo: false
notifications:
  email: false

rvm:
  - 2.1.3 # deployed
  - 2.3.1

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

script: bundle exec rake ci

jdk: oraclejdk8
