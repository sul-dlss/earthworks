<div data-controller="downloads" class="downloads">
  <h2 class="h4"><%= t("geoblacklight.download.downloads") %></h2>
  <ul class="list-unstyled">
    <% download_links.each do |link| %>
      <%= render link %>
    <% end %>
  </ul>
</div>
